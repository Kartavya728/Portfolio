"use client";

import React from "react";
import { useTheme } from "@/context/ThemeContext";

export const ThemeToggle = () => {
  // Theme is now automatic based on scroll position
  // This component is hidden but kept for potential future use
  return null;
};

export default ThemeToggle;
